name: vanta-bridge
version: 0.1.0
api-version: '1.21'
main: co.vanta.bridge.BridgePlugin
description: Bridges Minecraft chat + world events to the VANTA runtime.
author: vanta

# §1.1 chat commands (paper §7.5). All wired in subsequent commits;
# this scaffold registers the names so /<cmd> tab-completes from day 1.
commands:
  vanta:
    description: VANTA bridge status (debug).
    usage: /vanta
