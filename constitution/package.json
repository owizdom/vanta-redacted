{
  "name": "@vanta/constitution",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "VANTA constitution: autonomy envelope, gate floor, calibration baselines, fees, whitelists. The parameters the contract enforces and the agent reasons within (paper §3, §4, §6, §13).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "typecheck": "tsc -b"
  },
  "dependencies": {
    "@vanta/events": "workspace:*",
    "@vanta/tee": "workspace:*",
    "canonicalize": "3.0.0",
    "zod": "3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
