# Copy to .env.local (gitignored) and fill in.
# This key is loaded by vite.config.ts in dev and by the Vercel
# serverless function api/voice.ts in prod. It NEVER reaches the
# client bundle — the proxy at /api/voice is the only public surface.
ELEVENLABS_API_KEY=
