/**
 * VANTA — NPC supervisor (v0.1).
 *
 * Spawns the Wizard plus 2 ambient population bots (Sarah, Mike) inside the
 * same container. v1's seven-bot economy roster (whale, hedger, adversary,
 * curator, retail, tour guide, cartographer) is reduced to two ambient
 * wanderers for v0.1 — the visit-mode interactions those bots exercised
 * land in v0.2.
 */
import "./Wizard.js";
import "./Population.js";

console.log("[vanta-npcs] supervisor up; wizard + 2 ambient bots scheduling");
